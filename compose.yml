version: '3.9'

services:
  postgres:
    image: postgres
    container_name: devstorage_db
    restart: always
    environment:
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev
      - POSTGRES_DB=devstorage
    ports:
      - "5432:5432"